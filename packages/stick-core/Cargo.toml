[package]
name = "stick-core"
version = "0.1.0"
edition = "2021"
description = "Deterministic core game engine for StickHRPG (Degenerate Horizon) — stats, jobs, gacha RNG, tarot resonance & SysEx delta streaming."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
