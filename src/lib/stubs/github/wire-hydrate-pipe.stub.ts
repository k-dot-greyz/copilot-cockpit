/**
 * @stub MOD-WIRE-PIPE-HYDRATE
 * Replace github.ts mapPR with hydratePRCard from pipes/
 */
export function wireHydratePipe(): void {
  throw new Error(
    '[STUB] MOD-WIRE-PIPE-HYDRATE: delegate fetchOpenPRs mapping to hydratePRCard'
  );
}
