# GitHub OAuth Configuration
# Public Client ID (safe to bundle in client-side Astro app)
PUBLIC_GITHUB_CLIENT_ID=your_github_client_id_here

# Secure Client Secret (MUST NOT be bundled in client-side code; set only in Vercel/Cloudflare serverless environment)
GITHUB_CLIENT_SECRET=your_github_client_secret_here
